# python-adk-runtime — AI 維護指南（llm.txt）

此檔規範 AI 與協作者在維護 Python ADK Runtime（`python-adk-runtime/`）的流程，與 `AGENT.md`、`SPEC.md` 一致。

## 🎯 模組狀態 (2025年8月更新)
**當前狀態**: 生產就緒 ✅ (技術債務已清理完畢)
**完成度**: 96% - 核心功能驗證通過，準備進入生產部署階段
**關鍵修復**: RemoteTool proto 消息修復，Agent 團隊協作驗證完成

## 範圍與原則
- 嚴格遵守 Google ADK 標準：Agent、FunctionTool、Runner、SessionService
- 與 Go 平台解耦：僅透過 gRPC ToolBridge（RemoteTool）或 HTTP 互通
- 任何跨語言介面或型別變更先改 `contracts/`（SSOT），再回此專案對齊生成碼與相依
- 所有註解與文件使用繁體中文

## 必守規範（與 AGENT.md 對齊）

### Agent vs Tool 黃金準則
> **完整職責劃分說明**：參見 [../ARCHITECTURE.md - Agent vs Tool 職責劃分](../ARCHITECTURE.md#agent-vs-tool-職責劃分)

**核心原則**：
- **Agent 職責（決策層）**：WHY/WHAT/WHEN - 決策制定、工作流編排
- **Tool 職責（執行層）**：HOW/WHERE/WITH - 具體執行、數據操作

### 技術實現規範
3. **ADK 標準**：使用 `FunctionTool` 包裝函式，Agent 使用 Root + Sub-Agents 架構
4. **RemoteTool**：以 `grpc.aio` 連 ToolBridge；端點由 `DETECTVIZ_TOOLBRIDGE_ADDR` 指定；傳遞 `traceparent/tracestate`
5. **Session State**：透過 ADK `ToolContext` 管理狀態，不使用全域變數
6. **設定載入**：`detectviz_adk/config/loader.py` 與 Go Loader 對齊搜尋序與環境覆蓋
7. **模組卡**：新增/擴增元件需附 `module.card.json` 並通過 `contracts/tools/validate_module_card.py`
8. **安全**：Python 端不持有雲端寫入憑證；外部副作用集中於 Go 插件（Tools）
9. **SSOT 契約**：不得修改 `contracts/gen/**` 生成碼；如需型別/欄位調整，回到 `contracts/`

### MVP Phase 3 專用規範
10. **事後複盤聚焦**：當前開發重點為 postmortem_orchestrator ADK Agent 團隊
11. **混合架構決策**：業務邏輯（Python）+ 高性能查詢（Go）+ gRPC 橋接
12. **程式碼品質**：類型標註、docstring、測試覆蓋率 > 90%、錯誤處理完善

## 允許/禁止

### 允許的操作
- 新增 ADK Agent、FunctionTool、Runner（以 RemoteTool 呼叫 Go 插件）
- 實現符合 MVP Phase 3 的事後複盤功能
- 在 Python 端進行決策邏輯和業務流程編排
- 使用 RemoteTool 調用 Go 端的高性能數據查詢

### 嚴格禁止的操作
- **違反職責分離**：Agent 直接執行數據查詢、文件操作、API 調用
- **在 Python 端直接查詢外部系統**：如直接連接 InfluxDB、資料庫等
- 跨越 ADK 邏輯邊界、直連雲端憑證、繞過 Loader 讀寫設定
- 提交大型二進位、使用非 ADK 標準的代理架構
- 修改已定義的 contracts/ 契約（除非經過 SSOT 更新流程）

## 變更流程
1. 規劃：是否涉及 SSOT？若是，先改 `contracts/` 並 `buf lint && buf generate`。
2. 實作：使用 templates 腳手架建立元件；補 README 與最小測試。
3. 驗證：
```bash
# 啟動 Go ToolBridge（另窗）
# detectviz plugin serve --config ./go-platform/configs/config.yaml --http-demo --http-demo-listen :7777

# 執行 Python 端範例/測試（以虛擬環境）
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python server.py   # 或對應入口
```
4. 記錄：在根 `README.md`、文件站對應章節同步更新。
5. PR：說明 SSOT 影響、端到端流程（RemoteTool 呼叫、trace 傳遞）、是否更新文件。

## 提交前檢查清單

### SSOT 契約遵循
- [ ] 不改動 `contracts/gen/**` 生成碼
- [ ] 如有跨語言介面變更，已先更新 `contracts/` 並重新生成

### Agent vs Tool 職責檢查
- [ ] **Agent 職責驗證**：Agent 只做決策，不直接操作數據
- [ ] **Tool 職責驗證**：所有數據查詢、API 調用都通過 RemoteTool
- [ ] **混合架構檢查**：Python 端負責決策，Go 端負責執行

### 技術實現檢查
- [ ] RemoteTool 呼叫成功；trace context 具備
- [ ] Loader 搜尋序與環境覆蓋行為正確
- [ ] 模組卡通過驗證腳本
- [ ] ADK 標準遵循：正確使用 Agent、FunctionTool、Runner

### 品質保證檢查
- [ ] 程式碼品質：類型標註、docstring 完整
- [ ] 測試覆蓋率 > 90%，包含異常處理測試
- [ ] 錯誤處理完善，有降級機制
- [ ] README/文件已更新，與 AGENT.md 一致

### MVP Phase 3 特定檢查
- [ ] 功能符合事後複盤範圍，不包含 Phase 1/2 功能
- [ ] postmortem_orchestrator ADK Agent 團隊協作正確
- [ ] 與 Go 端 HealthAggregator 整合測試通過

## 🛠️ 技術債務預防 (2025年8月新增)

### 自動化驗證工具
```bash
# 運行完整整合測試
python test_adk_integration.py

# 檢查依賴項目新鮮度
pip list --outdated

# 驗證 RemoteTool 連接性
python -c "from detectviz_adk.tools.remote_tool import RemoteTool; print('RemoteTool OK')"
```

### Proto 消息正確性檢查
- [ ] RemoteTool 使用正確的 proto 消息: `pb.InvokeRequest` / `pb.InvokeResponse`
- [ ] 不使用已廢棄的消息類型: `ToolInvokeRequest` / `ToolInvokeReply`
- [ ] gRPC 存根匯入路徑正確: `contracts.gen.python.detectviz.contracts.v1.adk_bridge_pb2`

### 程式碼健康指標監控
- 代碼行數: 1936 行 (已測量，需維持精簡)
- 無 TODO/FIXME/HACK 標記 (已驗證清理完成)
- Agent 團隊配置: Root + 3 Sub-Agents (已驗證)
- 測試通過率: 100% (test_adk_integration.py)

### 效能基準線
- Agent 響應時間: < 30 秒
- 記憶體使用: < 512MB
- gRPC 連接建立: < 5 秒
- Session 狀態管理: 無記憶體洩漏

### 與最新 contracts/ 同步檢查
- [ ] proto 生成碼版本與 contracts/ 一致
- [ ] 所有新的枚舉值使用正確前綴 (ENUM_TYPE_PREFIX)
- [ ] RPC 方法呼叫使用標準化命名 (MethodRequest/MethodResponse)

## 🚨 緊急修復指南

### 常見問題快速診斷
1. **gRPC 連接失敗**:
   ```bash
   curl -s http://127.0.0.1:8081/readyz  # 檢查 go-platform
   echo $DETECTVIZ_TOOLBRIDGE_ADDR      # 驗證配置
   ```

2. **Proto 消息錯誤**:
   ```bash
   cd contracts && make gen  # 重新生成 proto 存根
   ```

3. **Agent 團隊故障**:
   ```python
   # 驗證 Agent 配置
   from detectviz_adk.agents.postmortem import postmortem_orchestrator
   print(f"Sub-agents: {[a.name for a in postmortem_orchestrator.sub_agents]}")
   ```

記住：任何修改都要先確保不破壞現有的 Agent 團隊協作模式！
